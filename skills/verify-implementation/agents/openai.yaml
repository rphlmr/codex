interface:
  display_name: "Verify Implementation"
  short_description: "Independently verify an implementation"
  default_prompt: "Independently verify the completed implementation against the final agreed plan and acceptance criteria."

policy:
  allow_implicit_invocation: true
